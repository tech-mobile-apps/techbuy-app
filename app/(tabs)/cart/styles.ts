import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: { flex: 1, backgroundColor: '#f5f5f5', paddingHorizontal: 14 },
  scrollView: { paddingTop: 16 },
  totalContainer: { marginTop: 16, marginBottom: 24 },
  emptyText: { color: '#908e8d', textAlign: 'center', marginTop: 24 },
  totalValue: {
    color: '#908e8d',
    fontSize: 20,
    fontWeight: 'bold',
    marginBottom: 16,
  },
});
