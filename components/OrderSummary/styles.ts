import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  mediumText: { fontSize: 16, color: '#000' },
  totalValue: { color: '#C084FC', fontWeight: '600', fontSize: 16 },
  totalContainer: { flexDirection: 'row', justifyContent: 'space-between', marginTop: 8 },
});
